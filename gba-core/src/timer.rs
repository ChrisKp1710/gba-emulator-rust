// Timer placeholder
pub struct Timer {
    // TODO: Implementazione timer
}

impl Timer {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Timer {
    fn default() -> Self {
        Self::new()
    }
}
